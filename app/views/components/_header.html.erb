<div class="d-flex flex-row align-items-center justify-content-between w-100">
    <div>
    <% if current_user%>
        Signed in as:
        <span class="font-weight-bold"><%= current_user.name %></span>
        <% if current_user.admin %>
            (admin)
        <% end %>
    <% end %>
    </div>
    <%= button_to "Sign out", sign_out_url, :class => "btn btn-primary" %>
</div>
